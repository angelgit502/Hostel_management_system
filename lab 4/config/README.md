# HMS Config Folder

This folder contains configuration files for database connection and environment variables.

### Files:
- `db.js` - MySQL connection pool configuration (to be connected with `mysql2` package).
